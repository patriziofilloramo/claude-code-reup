{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Reup VS Code Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}/extension"],
      "outFiles": ["${workspaceFolder}/extension/dist/**/*.cjs"],
      "preLaunchTask": "Build Reup VS Code Extension"
    }
  ]
}
