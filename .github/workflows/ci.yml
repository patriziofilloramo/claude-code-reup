name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  ci:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          persist-credentials: false

      - uses: actions/setup-node@v7
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install extension dependencies
        run: npm ci --prefix extension

      - name: Check version sync
        run: npm run check:version

      - name: Check formatting
        run: npm run format:check

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

      - name: Build VS Code extension
        run: npm run build:extension

      - name: Check product version policy
        if: runner.os == 'Linux'
        env:
          PRODUCT_VERSION_BASE: ${{ github.event.pull_request.base.sha || github.event.before }}
        run: node scripts/check-extension-version.mjs

      - name: Test
        run: npm test

      - name: Package VS Code extension
        run: npm run package:extension

      - name: Verify packaged VS Code extension
        run: npm run release:extension:check

      - name: Verify packaged VS Code extension from Git Bash
        if: runner.os == 'Windows'
        shell: bash
        run: npm run release:extension:check

      - name: Verify npm package dry-run
        if: runner.os == 'Linux'
        run: npm run release:package:check

      # Launches a real VS Code. It is the only place that can answer whether
      # the editor delivers the events the extension depends on — notably a
      # watcher on a single file outside the workspace, which retracts a stale
      # needs-input badge. A stub cannot answer that, and it had only ever been
      # confirmed on Windows.
      - name: VS Code extension host smoke test
        if: runner.os == 'Linux'
        run: xvfb-run --auto-servernum npm run test:extension-host

      - name: VS Code extension host smoke test
        if: runner.os != 'Linux'
        run: npm run test:extension-host
